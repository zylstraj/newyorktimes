import React from 'react';
import ReactDOM from 'react-dom';

const NewsStory = (props) => {
    const news = props.news.description;
    console.log(news);
    return(
    <div>
    <ul>
    </ul>
    Hello
    </div>
  )
}
export default NewsStory;
